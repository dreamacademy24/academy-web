# 드림아카데미 사이트 종합 점검 (2026-06-07)

손님 포털 / 어드민 예약·인보이스 / 어드민 운영도구 / 현지직원·공개사이트 4개 영역을 전수 점검한 결과입니다. (이미 이전에 고친 항목 — 신청목록 월/주차 그룹핑, 튜터 재배정 동기화, 신청 상세 stale 모달 등 — 은 제외)

우선순위: 🔴 즉시(효과 크고 안전) · 🟠 중간 · 🟢 낮음/장기

---

## A. 공통 기반 — 한 번에 전 화면이 좋아지는 것

- 🔴 **상태 피드백 통일** — 곳곳이 `alert()` 또는 silent fail(저장 실패해도 표시 없음). 공통 토스트 + 저장 버튼 "저장 중…/비활성" 처리로 통일. (포털·어드민 전반)
- 🔴 **디자인 토큰 적용 확산** — 이미 만든 `globals.css :root` + `lib/uiTokens.ts`를 페이지에 점진 적용해 색/버튼/배지/빈화면을 통일. 지금은 같은 상태인데 화면마다 색이 미묘하게 다름.
- 🟠 **공통 헬퍼 단일화** — 날짜 포맷(`fDate`/`fmtDate` 중복), 상태배지 색(페이지마다 ST/STATUS_META 별도 정의 → `lib/statusColors.ts`), `createClient` 직접 호출 → `lib/supabase` import 통일.
- 🟠 **로딩/빈상태 표준화** — 로딩 스피너·"내역 없음" 문구가 화면마다 제각각. 공통 컴포넌트로.
- 🟠 **모바일 반응형** — 손님 폼·현지직원 화면 480~768px 구간 점검(표 가로스크롤, 탭 라벨 잘림).
- 🟢 **다크모드**(장기), **접근성(WCAG 대비)**(장기).

## B. 손님 포털

- 🔴 **항공편 입력 경로 혼란** — `my-booking`·`portal/flight`·`checkin-detail` 세 곳에 항공편 입력이 분산. 한 곳으로 모으고 나머지는 안내 링크만.
- 🔴 **결제/신청 후 피드백** — 결제 후 상태 갱신에 로딩 표시 없음, 셔틀/픽업/튜터 신청은 `alert`만. 토스트로 통일.
- 🟠 **학생이름 잘림** — 여러 카드에서 "한글(영문)" 이름이 잘림 + title 툴팁 없음.
- 🟠 **튜터 신청 일정 UI** — 요일 체크 + 시간 텍스트 + 시작/종료일이 흩어져 있음. 통합.
- 🟠 **날짜 입력 사용성** — `<input type=date>` 모바일 폴백/안내.
- 🟢 빈상태 문구 통일, 모달 X 버튼, 폰트 focus 색 통일.

## C. 어드민 예약 / 인보이스

- 🔴 **확정예약 탭 컬럼 과다(16개·가로스크롤)** — 핵심 컬럼(예약번호·예약자·학생·체크인·확인)만 기본 표시, 나머지는 토글 또는 상세에서. 직원이 가장 많이 보는 화면.
- 🔴 **누락 배지 색 오해** — 누락 항목(항공/아이디/인보이스/잔금)이 빨강이라 "문제"처럼 보임. 중립색/아이콘으로.
- 🟠 **학생관리 달력 가독성** — 대규모 월에 셀 높이 폭증·폰트 작음(9px). 고정 높이+더보기 or 월 네비.
- 🟠 **표기 통일 마무리** — 나이 `fmtAge` 일부만 적용, 항공편 단순텍스트 vs 구조화필드 혼재, 상태배지 색 두 화면 불일치. 통일.
- 🟠 **예약상세에 영문명·추가보호자 표시** — 필드는 저장되는데 기본정보 탭에 표시/편집 영역 없음.
- 🟠 **인원 요약 컬럼** — 부킹리스트에 "보호자+아이"(예 2+1) 요약 추가하면 패키지 파악 빠름.
- 🟢 신규예약 모달 저장 후 폼 초기화(연속 등록 시 데이터 혼입 위험), 예약상세 7탭 모바일 잘림, 특이사항 펼침 표시 강화.

## D. 어드민 운영 도구

- 🔴 **튜터 수업 시간 미설정(`--:--`) 근본 원인** — 확정 시 `class_time` 누락되는 패턴 점검(주간 스케줄에 시간 빈 카드 생김).
- 🟠 **`/admin/tutors`(구) 스케줄 탭 데이터 원천 확인** — 화상영어(online_class_enrollments)를 조회하는 듯. 드림하우스 튜터와 혼동 소지. 현재 주 경로는 `/admin/tutor-class`라 구 페이지 정리/통합 검토.
- 🟠 **SSP·하우스보고 피드백/레이아웃** — 상태 변경 silent fail 위험, 보고 보기 탭 과밀.
- 🟠 **tour-shuttle / afterschool 신청목록 월·주차 그룹핑 재확인** — 적용돼 있으나 라이브 점검 권장.
- 🟢 hub 카드 설명 잘림(툴팁), 인쇄 CSS 미세조정, 수업 종료 후 상태 자동전이 없음.

## E. 현지직원 + 공개 사이트

- 🔴 **현지직원 학생이름 영어 + 잘림 처리** — (afterschool는 이미 영어 적용함) tutor-class 주간 그리드 이름 줄바꿈/잘림 정리.
- 🟠 **요일 표기 enum 통일** — 한글/영문/약어 변환 함수가 섞여 있음 → SUN~SAT 단일.
- 🟠 **ashuttle 주소 정규화** — 룸/주소 입력 대소문자·띄어쓰기 제각각(b17L17 vs B17 L15). 입력 검증.
- 🟠 **공개 페이지** — 이미지 `loading="lazy"`, 모바일 hero 글씨, CTA 링크 통일(카카오/booking 혼재), footer 대비.
- 🟢 현지직원 로딩 스피너, Back 버튼 스타일 통일.

## F. 추가하면 좋을 기능 (제안)

- 🔴 **통합 검색** — 예약자·학생·전화번호를 한 곳에서 검색 → 예약 상세로 점프.
- 🟠 **오늘 대시보드 확장** — (이미 /admin/today 만듦) 픽업·셔틀·체크인·마감 위젯 보강.
- 🟠 **엑셀 내보내기 확대** — 확정예약/학생/튜터 정산.
- 🟠 **텔레그램 정기 알림** — 매일 아침 오늘 일정 요약(드림보드 ⑤).
- 🟠 **결제상태 enum 정식화** — 지금은 텍스트 "완료" 포함 여부로 판정(느슨함).
- 🟢 손님 푸시 PWA 재구독 안정화, 다국어(한/영/일), 수업종료 자동 상태전이.

---

## 추천 진행 순서

1. **공통 기반 1차** — 토스트/저장피드백 통일 + 디자인 토큰 확산(색·버튼·배지). 전 화면 체감 큼, 위험 낮음.
2. **확정예약 탭 정리** — 컬럼 축소 + 누락배지 색 + 인원 요약. 직원 일상 효율.
3. **표기 통일 마무리** — 나이/항공편/상태배지/룸번호 잔여.
4. **튜터 시간 미설정 근본수정** + 구 tutors 페이지 정리.
5. **통합 검색**(신규 기능).
6. 손님 포털 항공편 경로/일정 UI 정리.
7. 공개 페이지·현지직원 다듬기, 이후 다국어/다크모드(장기).

> 각 항목은 페이지·라인 근거가 점검 원본에 있으니, 진행할 항목 고르시면 해당 파일부터 바로 손보겠습니다.
