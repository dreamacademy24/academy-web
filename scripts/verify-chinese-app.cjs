const {chromium}=require('C:/Users/desko/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
const http=require('node:http'),fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
(async()=>{const root=path.resolve('public');const server=http.createServer((req,res)=>{const p=path.resolve(root,'.'+decodeURIComponent(req.url.split('?')[0]));if(!p.startsWith(root+path.sep)){res.writeHead(403).end();return;}fs.readFile(p,(e,b)=>{if(e){res.writeHead(404).end();return;}res.setHeader('Content-Type',p.endsWith('.mp3')?'audio/mpeg':p.endsWith('.html')?'text/html; charset=utf-8':p.endsWith('.pdf')?'application/pdf':'application/json');res.end(b);});});await new Promise(r=>server.listen(0,'127.0.0.1',r));const base=process.argv[2]||'http://127.0.0.1:'+server.address().port;const browser=await chromium.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});try{const page=await browser.newPage({viewport:{width:1024,height:768}});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(base+'/mandarin/index.html');assert.equal(await page.locator('.tile').count(),84);await page.locator('[data-action=speak][data-id="014"]').click();await page.waitForFunction(()=>audio&&!audio.paused&&audio.duration>0);console.log('Real Chinese MP3 playback passed.');await page.locator('#search').fill('apple');assert.equal(await page.locator('.tile').count(),2);await page.locator('#search').fill('014');assert.equal(await page.locator('.tile').count(),1);await page.getByRole('button',{name:'Flip apple card'}).click();assert.equal(await page.locator('.card .zh').textContent(),'苹果');await page.getByRole('button',{name:'Mark apple as practiced'}).click();await page.reload();assert.match(await page.locator('#stars').textContent(),/1/);await page.locator('#group').selectOption('7');assert.equal(await page.locator('.tile').count(),6);await page.locator('#quizBtn').click();assert.equal(await page.locator('[data-choice]').count(),3);const correct=await page.evaluate(()=>question.id);await page.locator('[data-choice="'+correct+'"]').click();assert.match(await page.locator('#quizFeedback').textContent(),/Yes!/);await page.locator('#quizBtn').click();await page.locator('#group').selectOption('all');await page.screenshot({path:'tmp/chinese-review/app-tablet.png'});for(const [w,h] of [[768,1024],[390,844],[1180,820]]){await page.setViewportSize({width:w,height:h});assert(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth),'Horizontal overflow at '+w);}await page.setViewportSize({width:390,height:844});await page.screenshot({path:'tmp/chinese-review/app-phone.png'});for(const name of ['cards-duplex','starter-workbook','printing-guide']){const r=await page.request.get(base+'/mandarin/'+name+'.pdf');assert.equal(r.status(),200);assert((await r.body()).subarray(0,4).equals(Buffer.from('%PDF')));}const words=JSON.parse(fs.readFileSync('public/mandarin/vocabulary.json'));for(const word of words){const r=await page.request.get(base+'/mandarin/audio/'+word.id+'.mp3');assert.equal(r.status(),200);assert((await r.body()).length>1000);}assert.deepEqual(errors,[]);console.log('84 cards/audio files, flip, search, filters, persistent stars, quiz, three tablet/mobile sizes, PDFs and JS errors: PASS.');}finally{await browser.close();await new Promise(r=>server.close(r));}})().catch(e=>{console.error(e);process.exit(1)});
