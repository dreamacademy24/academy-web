# Student care preview release — preparation

Date: 2026-09-12. Production deployment is not authorized by the current request; a separate test preview is authorized.

## Prepared

- Shared student roster, visit history and teacher assignment changes are committed in 9c5e2fa.
- Korean and English guides are generated by `node scripts/build-student-care-guides.mjs` into `/staff-guides/student-care/ko.html` and `en.html`. Shared print styling includes a browser Print / Save PDF button. Existing staff manuals remain unchanged.
- Added guide links to the student page and both staff-home entry points.
- Public learning stage labels changed to DSL-F2. Slower English speech uses rate 0.7.
- Full Next production build succeeded with 199 pages. Browser inspection confirmed Korean guide rendering; English language switching, six sections, valid in-page links, and no horizontal overflow at the inspected desktop width.
- Existing middleware deprecation and workspace-root warning remain. This build check used the local environment; it is not deployment verification.

## Deployment access and data isolation

- Vercel connector returned 403 for academy-web in dreamacademy. User signed into the Vercel browser UI, which now has project access.
- Observed production deployment: ce02d509159a2ce331f2eef6507136161d71230f on main, Ready. No production aliases or deployments have been changed.
- Existing Supabase has only the production project `yiglafscjvjgkxpycevk` and no development branch.
- Vercel variables currently include Supabase credentials in All Environments and mail/payment/notification settings shared with Preview. Do not push this branch to trigger a preview until test settings have been isolated.
- Awaiting the user's organization choice for a separate test DB, then the Supabase cost-confirmation flow. No paid branch or new project has been created.
- No real staff/student records should be copied merely to test. Use isolated fictional students and separate test staff accounts, with teacher roles and password verification representative of production.

## Before deployment

1. Confirm test DB organization and quoted cost; provision the test branch/environment.
2. Check the new branch's schema and migration history before applying anything. Legacy schema may not all be represented by local migration files; do not blindly replay production migrations or recreate existing tables.
3. Apply missing care migrations to the test DB only, including `20260912061214_student_care_teacher_assignments.sql`. Seed minimal fictional test records and test login accounts.
4. Configure branch-specific Vercel Preview variables. Never overwrite Production or unrelated branches. Separate Supabase URL, browser key, server key and staff signing secret. Disable outbound mail, payment and notification actions in the test scope so testers cannot trigger production services.
5. Push a non-production branch, verify the resulting SHA and Preview target, then wait for Ready. Do not promote to Production.
6. Verify real preview requests: unauthenticated denial; test administrator login, source confirmation and revisit; local teacher assigned-only roster, removal, disabled-account rejection; Korean/English guides and learning flows; mobile and recording permissions.
7. Record exact URL, SHA, test results and remaining limits here. Provide preview credentials privately; never add passwords or live API keys to this guide or repository.
8. After deployment and verification, inspect existing staff notices to avoid duplicates. Publish a normal `[새 기능 안내]` / `[수정 안내]` notice describing the test scope and guide links if appropriate for the available audience. Do not republish the previous manual or change read/done states.

## Current status

**Not deployed.** Guides are available locally; preview DB and branch-specific environment setup remain pending. New assignment migration is still not applied to production.
